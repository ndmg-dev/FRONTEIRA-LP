# Copie para .env e preencha antes de subir o serviço (§B.7 — nenhum segredo
# tem default de produção; este arquivo nunca deve conter valores reais).

DATABASE_URL=postgresql+psycopg://fronteira:fronteira@localhost:5432/fronteira
RESEND_API_KEY=
EMAIL_FROM=Fronteira <contato@seudominio.com.br>
TEAM_INBOX=comercial@seudominio.com.br
IP_HASH_PEPPER=troque-por-um-segredo-aleatorio-longo
ALLOWED_ORIGINS=http://localhost:5173
RATE_LIMIT_PER_HOUR=20
